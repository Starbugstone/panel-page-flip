# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
APP_DATA_KEY='test_app_data_key_change_in_real_envs'
SYMFONY_DEPRECATIONS_HELPER=disabled
PANTHER_APP_ENV=panther
PANTHER_ERROR_SCREENSHOT_DIR=./var/error-screenshots

# Test database — uses the same MySQL service but a separate schema (Doctrine appends "_test")
DATABASE_URL="mysql://${MYSQL_USER:-cbz_user}:${MYSQL_PASSWORD:-cbz_password}@database:3306/cbz_reader?serverVersion=8.0&charset=utf8mb4"

CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
MAILER_DSN=null://null
MESSENGER_TRANSPORT_DSN=in-memory://
LOCK_DSN=flock

DROPBOX_APP_KEY=test_app_key
DROPBOX_APP_SECRET=test_app_secret
DROPBOX_REDIRECT_URI=http://localhost/dropbox/callback
DROPBOX_APP_FOLDER=/comics
DROPBOX_SYNC_LIMIT=3
DROPBOX_RATE_LIMIT=10
MAX_CONCURRENT_UPLOADS=3

APP_URL=http://localhost:8080
MAILER_FROM_ADDRESS=test@example.com
MAILER_FROM_NAME="Test Sender"

# Alerts are on here — the production default is off, and a suite that ran with
# the default could never prove that a threshold fires. The recipient is
# explicit so no test depends on which admin accounts happen to exist.
SECURITY_ALERTS_ENABLED=1
SECURITY_ALERT_EMAILS=security-alerts@example.test
SECURITY_ALERT_WINDOW_MINUTES=15
# Deliberately far below the production defaults: a test that has to make ten
# real HTTP requests to reach a threshold is testing patience. What matters is
# that the configured number is the one honoured.
SECURITY_ALERT_FAILED_LOGIN_THRESHOLD=3
SECURITY_ALERT_AUTHZ_THRESHOLD=3

APP_LOG_RETENTION_DAYS=30
SECURITY_LOG_RETENTION_DAYS=365
AUDIT_LOG_RETENTION_DAYS=365
