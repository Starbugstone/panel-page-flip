/** The Offerwall target: explanatory text and two buttons, no user content. */
export const BULK_UPLOAD_GATE_ROUTE = "/upload/bulk";

/** The batch itself — filenames, progress, failures — and permanently ad-free. */
export const BULK_UPLOAD_QUEUE_ROUTE = "/upload/bulk/session";

export const SINGLE_UPLOAD_ROUTE = "/upload";
